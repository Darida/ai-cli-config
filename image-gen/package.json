{
  "name": "image-gen",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-asset": "vite-node --script generate-asset.ts",
    "list-image-models": "vite-node --script list-image-models.ts",
    "pick-image-model": "vite-node --script pick-image-model.ts",
    "clean-image": "vite-node --script clean-image.ts",
    "test": "vitest run",
    "test:pricing": "vitest run image-cost-estimate.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "sharp": "^0.35.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "overrides": {
    "rollup": "4.62.2"
  }
}
