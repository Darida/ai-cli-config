# Antigravity CLI config

Check the documentation at https://antigravity.google/docs/home before changing anything in this directory — the config schema (`settings.json`, `hooks.json`, etc.) is Antigravity-specific and doesn't necessarily match Claude Code's.
