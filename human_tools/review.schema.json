{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["LGTM", "ACTION_REQUIRED"]
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "rule": {
            "type": "string",
            "description": "Rule name (e.g. Rule 1: Review Comments)"
          },
          "file": {
            "type": "string",
            "description": "Path (relative to repo root) of the file containing the violation. If the finding spans multiple files, use the single most representative file here and mention the others in text."
          },
          "text": {
            "type": "string",
            "description": "Actionable finding description"
          }
        },
        "required": ["rule", "file", "text"],
        "additionalProperties": false
      }
    }
  },
  "required": ["status", "notes"],
  "additionalProperties": false
}
